# v0.1 Initial Executed Results

Status: **executed / partial metric coverage**

## Provenance

- GitHub Actions workflow: `v0.1 checks`
- workflow run: `31171905710`
- validation PR head: `f2e5eebf3d86284649e183e4da0dd7f7e84b8bfd`
- PR merge-test commit: `2bc1b9cf0062a5ce20b05b8aa36e20f43945d401`
- Python: `3.11.15`
- test job conclusion: `success`
- unit tests: `8 / 8` passed

The run executed:

```text
python -m pip install -e .
python -m unittest discover -s tests -v
python -m negative_space_search.run_v0_1
```

## Scope

This is **not** a full `Q_Psi` result. The current runner reports only the dimensions the initial toy benchmark implements cleanly:

- initial action match;
- supported causal diagnosis;
- required evidence-action match;
- healthy-absence preservation;
- hostile observational-equivalence behavior.

`Q_CF`, ecological durability, history-based search improvement, and full cost-normalized metrics are not yet implemented and are therefore not scored here.

## Initial five-case diagnostic counts

| Policy | Action correct | Diagnosis supported | Required evidence matches | Healthy absence preserved |
| --- | ---: | ---: | ---: | ---: |
| random action | 2 / 5 | 0 / 5 | 0 | 0 / 1 |
| gap heuristic | 1 / 5 | 0 / 5 | 0 | 0 / 1 |
| novelty heuristic | 2 / 5 | 0 / 5 | 0 | 0 / 1 |
| performance heuristic | 3 / 5 | 0 / 5 | 0 | 1 / 1 |
| self-confirming opportunity search | 1 / 5 | 0 / 5 | 0 | 0 / 1 |
| general causal reasoner | 4 / 5 | 4 / 5 | 2 | 1 / 1 |
| causal negative-space search | 5 / 5 | 4 / 5 | 2 | 1 / 1 |

The runner currently reports required-evidence matches as a count. In the five canonical cases, explicit evidence actions are expected for B, D, and E, so `2` means two of those three expected evidence selections matched.

## Hostile observational-equivalence test

The hostile pair supplies identical initial observations for:

```text
World I: underinvestment
World S: justified selection
```

A pass requires non-guessing behavior plus the discriminating evidence action `controlled_external_value_test`.

| Policy | Hostile pair |
| --- | --- |
| random action | fail |
| gap heuristic | fail |
| novelty heuristic | fail |
| performance heuristic | fail |
| self-confirming opportunity search | fail |
| general causal reasoner | **pass** |
| causal negative-space search | **pass** |

### Interpretation

The hostile test supports the narrower claim that explicit causal policies can recognize non-identifiability and request discriminating evidence instead of converting absence directly into intervention.

It does **not** establish an incremental advantage for causal negative-space search over strong general causal reasoning: both policies passed this test.

This is an important constraint on the project claim. The hostile pair is currently a useful anti-naive-search benchmark, not evidence that the explicit `Psi_NS` architecture is uniquely necessary.

## Model-inadequate case

The causal negative-space policy chose the expected high-level action (`abstain`) on `E_model_inadequate`, but:

```text
diagnosis_supported = false
evidence_match = false
```

So the policy reached a conservative action without correctly localizing the failure to causal-vocabulary inadequacy or requesting the intended `cross_interface_probe`.

The general causal reasoner also failed the model-inadequate case at the action, diagnosis, and evidence-selection level.

This result should be preserved before any policy modification.

## Current evidential status

What this run supports:

- the repository installs and executes;
- the hostile observational-equivalence environment behaves as designed;
- naive gap/opportunity policies are exposed by the healthy-absence and hostile controls;
- both strong causal policies know not to guess on the hostile pair.

What this run does **not** support:

- that causal negative-space search outperforms a strong general causal reasoner overall;
- that the model-inadequacy mechanism is solved;
- counterfactual causal fidelity;
- durability after ecological response;
- history-based improvement of `Psi`;
- real-world generalization.

## Next empirical target

Do not repair the theory in response to this run.

The next implementation target is to make the model-inadequacy condition genuinely discriminative and test whether a policy can distinguish:

```text
ordinary uncertainty
```

from:

```text
current causal vocabulary is inadequate
```

without exposing the answer directly in the observation.