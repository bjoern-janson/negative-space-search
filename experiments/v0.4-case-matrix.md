# v0.4 Frozen Case Matrix

Status: **frozen before policy implementation**

Each episode exposes three unlabeled numeric measurement pairs plus one scalar surface hint. The stable relation is not named in the raw record.

## Acquisition episodes

| ID | pair 0 | pair 1 | pair 2 | surface_hint | prediction | outcome | resolving_probe |
| --- | --- | --- | --- | ---: | ---: | ---: | --- |
| `A_D1` | `(0.42, 0.46)` | `(0.30, 0.48)` | `(0.30, 0.46)` | `0.85` | `0.80` | `0.20` | `independence_probe` |
| `A_D2` | `(0.42, 0.46)` | `(0.30, 0.48)` | `(0.30, 0.46)` | `0.82` | `0.70` | `0.10` | `independence_probe` |
| `A_P1` | `(0.18, 0.38)` | `(0.10, 0.90)` | `(0.22, 0.43)` | `0.02` | `0.60` | `0.00` | `payoff_regime_probe` |
| `A_P2` | `(0.18, 0.38)` | `(0.10, 0.90)` | `(0.22, 0.43)` | `-0.04` | `0.50` | `-0.10` | `payoff_regime_probe` |
| `A_N1` | `(0.16, 0.35)` | `(0.28, 0.46)` | `(0.32, -0.32)` | `-0.86` | `0.40` | `-0.20` | `topology_probe` |
| `A_N2` | `(0.16, 0.35)` | `(0.28, 0.46)` | `(0.32, -0.32)` | `-0.81` | `0.30` | `-0.30` | `topology_probe` |

All acquisition episodes have absolute prediction error `0.60`.

The cheap surface cue perfectly separates the three resolving probes:

```text
high      -> independence_probe
near_zero -> payoff_regime_probe
low       -> topology_probe
```

The stable pairwise relations are simultaneously present:

```text
pair 0 close         -> independence_probe
pair 1 far           -> payoff_regime_probe
pair 2 sign_disagree -> topology_probe
```

## Repair episodes

| ID | pair 0 | pair 1 | pair 2 | surface_hint | prediction | outcome | resolving_probe |
| --- | --- | --- | --- | ---: | ---: | ---: | --- |
| `B_D` | `(0.42, 0.46)` | `(0.30, 0.48)` | `(0.30, 0.46)` | `-0.78` | `0.44` | `-0.16` | `independence_probe` |
| `B_P` | `(0.18, 0.38)` | `(0.10, 0.90)` | `(0.22, 0.43)` | `0.84` | `0.59` | `-0.01` | `payoff_regime_probe` |
| `B_N` | `(0.16, 0.35)` | `(0.28, 0.46)` | `(0.32, -0.32)` | `0.00` | `0.79` | `0.19` | `topology_probe` |

All repair episodes also have absolute prediction error `0.60`.

The repair phase deliberately permutes the previously rewarded surface association while preserving the stable pairwise relation.

## Held-out cases

| ID | pair 0 | pair 1 | pair 2 | surface_hint | prediction | outcome | correct_probe |
| --- | --- | --- | --- | ---: | ---: | ---: | --- |
| `R_DEP` | `(0.42, 0.46)` | `(0.30, 0.48)` | `(0.30, 0.46)` | `-0.80` | `0.78` | `0.18` | `independence_probe` |
| `R_PAY` | `(0.18, 0.38)` | `(0.10, 0.90)` | `(0.22, 0.43)` | `0.85` | `0.45` | `-0.15` | `payoff_regime_probe` |
| `R_NET` | `(0.16, 0.35)` | `(0.28, 0.46)` | `(0.32, -0.32)` | `0.00` | `0.58` | `-0.02` | `topology_probe` |
| `R_NOVEL` | `(0.20, 0.40)` | `(0.30, 0.50)` | `(0.25, 0.45)` | `0.85` | `0.61` | `0.01` | `model_disrupting_probe` |

All held-out cases have absolute prediction error `0.60`.

Under compressed prediction/outcome/error similarity after all nine resolved episodes, the nearest historical cases are frozen as:

```text
R_DEP   -> B_N  -> topology_probe
R_PAY   -> B_D  -> independence_probe
R_NET   -> B_P  -> payoff_regime_probe
R_NOVEL -> A_P1 -> payoff_regime_probe
```

All four are wrong.

## Expected representation repair

Before the repair episodes, the preregistered parsimony-penalized relation learner is expected to select:

```text
independence_probe -> hint_high
payoff_regime_probe -> hint_near_zero
topology_probe -> hint_low
```

After the repair episodes, it is expected to select:

```text
independence_probe -> pair0_close
payoff_regime_probe -> pair1_far
topology_probe -> pair2_sign_disagree
```

These expectations are frozen as testable predictions, not implementation requirements to patch toward after execution.
