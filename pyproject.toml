[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "negative-space-search"
version = "0.1.0"
description = "Benchmarking causal negative-space search in adaptive epistemic systems"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
